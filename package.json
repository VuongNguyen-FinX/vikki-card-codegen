{
  "name": "vikki-card-codegen",
  "version": "1.0.0",
  "description": "AST codemod that wires a new prepaid card scheme variant into vikki-host-app source (mirrors PR #3215).",
  "bin": {
    "gen-card-scheme": "bin/gen-card-scheme.js"
  },
  "files": [
    "bin"
  ],
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc"
  },
  "dependencies": {
    "ts-morph": "^22.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  },
  "packageManager": "pnpm@11.17.0",
  "license": "MIT"
}
